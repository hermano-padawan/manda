import type { Metadata } from "next";import Link from "next/link";import Header from "../ui/Header";import {mandalas} from "../data";
export const metadata:Metadata={title:"Mandalas para imprimir gratis",description:"Cinco mandalas originales en blanco y negro, listos para imprimir y colorear.",alternates:{canonical:"/mandalas"}};
export default function Mandalas(){return <><Header/><main className="shell"><nav className="crumbs" aria-label="Migas de pan"><Link href="/">Inicio</Link><span>→</span><span>Mandalas</span></nav><section className="intro compact"><p className="eyebrow">Colección 01</p><h1>Mandalas para imprimir</h1><p>Elige un dibujo, abre su lámina y pulsa imprimir. Todos están preparados en blanco y negro para que el color lo pongas tú.</p></section><div className="drawing-grid">{mandalas.map((m)=><Link className="drawing-card" href={`/mandalas/${m.slug}`} key={m.slug}><span className="drawing-image"><img src={m.image} alt={`${m.title} para imprimir y colorear`}/></span><strong>{m.title}</strong><small>{m.short}</small></Link>)}</div><section id="como-imprimir" className="note"><h2>Cómo imprimirlos</h2><p>Abre el dibujo que prefieras, selecciona «Imprimir» y usa papel A4. Para colorear con rotuladores, elige un papel de mayor gramaje.</p></section></main></>}
