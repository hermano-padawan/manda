import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import Header from "../../ui/Header";import {mandalas} from "../../data";import Actions from "./Actions";
export function generateStaticParams(){return mandalas.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const m=mandalas.find(x=>x.slug===slug);if(!m)return{};return{title:`${m.title} para imprimir`,description:m.description,alternates:{canonical:`/mandalas/${m.slug}`},openGraph:{images:[m.image]}}}
export default async function Drawing({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const m=mandalas.find(x=>x.slug===slug);if(!m)notFound();const related=mandalas.filter(x=>x.slug!==m.slug).slice(0,3);const jsonLd={"@context":"https://schema.org","@graph":[{"@type":"ImageObject",name:m.title,description:m.description,contentUrl:`https://dibulisto.site${m.image}`,license:"https://creativecommons.org/licenses/by-nc/4.0/"},{"@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Inicio",item:"https://dibulisto.site/"},{"@type":"ListItem",position:2,name:"Mandalas",item:"https://dibulisto.site/mandalas"},{"@type":"ListItem",position:3,name:m.title,item:`https://dibulisto.site/mandalas/${m.slug}`}]}]};return <><Header/><main className="shell"><nav className="crumbs" aria-label="Migas de pan"><Link href="/">Inicio</Link><span>→</span><Link href="/mandalas">Mandalas</Link><span>→</span><span>{m.title}</span></nav><article className="drawing-detail"><header><p className="eyebrow">Lámina para colorear</p><h1>{m.title} para imprimir</h1><p>{m.description}</p><Actions/></header><figure><img src={m.image} alt={`${m.title} en blanco y negro para imprimir y colorear`}/><figcaption>Formato cuadrado · impresión recomendada en A4</figcaption></figure></article><section className="related"><div className="section-title"><h2>Otros mandalas</h2><Link href="/mandalas">Ver todos</Link></div><div className="related-grid">{related.map(x=><Link href={`/mandalas/${x.slug}`} key={x.slug}><img src={x.image} alt=""/><strong>{x.title}</strong></Link>)}</div></section><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/></main></>}
